[project]
name = "flm-modules"
version = "0.1.0"
description = "Reusable neural network building blocks."
requires-python = ">=3.12"
dependencies = [
  "torch>=2.5",
]

[project.optional-dependencies]
nvidia = [
  "tilelang>=0.1.11",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
